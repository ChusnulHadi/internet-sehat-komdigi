# Copy ke .env lalu isi nilai asli. .env di-gitignore (jangan commit).
# Dipakai docker-compose.yml untuk override default "changeme".

# Password web/console dnsdist
DNSDIST_DASHBOARD_PASSWORD=changeme

# API key REST dnsdist — dipakai dnsdist DAN dashboard, harus sama
DNSDIST_API_KEY=changeme
